__pycache__/
*.py[cod]
.venv/
.vscode/settings.json
.DS_Store
~$*.xlsx

# RE-DASH.1 -- outputs/dashboard.html is a derived artifact,
# regenerated by generate_dashboard.py on every run. Never versioned,
# same principle Articulo 2 already applies to data.
outputs/

# RE-DASH.1.8 -- logs/ is written by the launchd auto-regen job
# (scripts/com.armando.sop-dashboard-regen.plist). Runtime output,
# never versioned, same principle as outputs/.
logs/
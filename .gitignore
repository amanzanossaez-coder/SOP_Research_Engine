__pycache__/
*.py[cod]
.venv/
.vscode/settings.json
.DS_Store

# RE-DASH.1 -- outputs/dashboard.html is a derived artifact,
# regenerated by generate_dashboard.py on every run. Never versioned,
# same principle Articulo 2 already applies to data.
outputs/